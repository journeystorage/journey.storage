{
  "name": "journey-storage",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:web": "turbo dev --filter=@journey/web",
    "dev:consulting": "turbo dev --filter=@journey/consulting",
    "dev:investors": "turbo dev --filter=@journey/investors",
    "build": "npm run build -w @journey/web && rm -rf .next server.js && cp apps/web/.next/standalone/apps/web/server.js . && cp -r apps/web/.next/standalone/apps/web/.next .next && cp -r apps/web/.next/static .next/static && cp -r apps/web/public/. public/",
    "build:web": "npm run build -w @journey/web && rm -rf .next server.js && cp apps/web/.next/standalone/apps/web/server.js . && cp -r apps/web/.next/standalone/apps/web/.next .next && cp -r apps/web/.next/static .next/static && cp -r apps/web/public/. public/",
    "build:consulting": "npm run build -w @journey/consulting && rm -rf .next server.js && cp apps/consulting/.next/standalone/apps/consulting/server.js . && cp -r apps/consulting/.next/standalone/apps/consulting/.next .next && cp -r apps/consulting/.next/static .next/static && cp -r apps/consulting/public/. public/",
    "start": "node server.js",
    "lint": "turbo lint"
  },
  "dependencies": {
    "next": "^16.2.1"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
