// Rent-a-space pages use the same site footer as the rest of the site, so the
// NAP blocks, link columns and legal links never drift between page types.
export { default } from '@/components/layout/SiteFooter'
