// The site footer is shared by every page (homepage, legal, move-out, and the
// rent-a-space hub + facility pages via RentFooter). See SiteFooter.tsx.
export { default } from '@/components/layout/SiteFooter'
