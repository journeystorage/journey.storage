import { ChatPanel } from '@/components/ChatPanel'

export default function ChatPage() {
  return (
    <ChatPanel
      name="Jarvis"
      subtitle="Sees everything, every department."
      emptyHint="Ask about your open tasks, notes, or anything Journey Storage. Jarvis can see across every department."
    />
  )
}
