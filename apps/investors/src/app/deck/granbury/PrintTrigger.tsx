'use client'

/**
 * PrintTrigger — no overlay, just auto-triggers browser print dialog
 * after a delay to allow images to load.
 *
 * For pixel-perfect PDF, use: node scripts/export-deck-pdf.mjs
 */
export default function PrintTrigger() {
  return null
}
