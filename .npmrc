legacy-peer-deps=true
# puppeteer is a dev-only screenshot tool; the production build never needs
# Chrome. Skip the browser download so Hostinger's `npm install` doesn't fail.
puppeteer_skip_download=true
